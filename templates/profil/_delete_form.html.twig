<form method="post" action="{{ path('admin_poc_delete', {'id': poc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ poc.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
